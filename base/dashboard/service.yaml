apiVersion: v1
kind: Service
metadata:
  name: deployment-dashboard
  labels:
    app: deployment-dashboard
spec:
  type: NodePort
  selector:
    app: deployment-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30090
